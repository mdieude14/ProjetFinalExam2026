# Modele de configuration du front-end.
# Copier en « .env ». Seules les variables VITE_ sont exposees au navigateur,
# et tout ce qui figure ici est PUBLIC : aucune cle secrete.

VITE_API_URL=/api
VITE_STRIPE_PUBLIC_KEY=
